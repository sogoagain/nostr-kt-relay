ktor {
  deployment {
    port = 8080
  }
  application {
    modules = [com.sogoagain.relay.ApplicationKt.module]
  }
}

database {
  embedded = true
  url = "jdbc:h2:mem:test;DB_CLOSE_DELAY=-1"
  user = "root"
  password = ""
}
