ktor {
  deployment {
    port = 8080
  }
  application {
    modules = [com.sogoagain.relay.ApplicationKt.module]
  }
}

database {
  url = ${DB_URL}
  user = ${DB_USER}
  password = ${DB_PASSWORD}
}
